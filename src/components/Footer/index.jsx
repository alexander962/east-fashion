import React from 'react'
import cl from 'classnames'

import styles from './index.module.scss'
import {SocialNetworks} from "@/components";
const Footer = ({className = ''}) => {
  return (
    <div>
      <SocialNetworks className='' />
    </div>
  )
}

export default Footer;
